{
  "name": "wacko-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  },
  "pnpm": {
    "overrides": {
      "vite": "$vite"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}